<!-- <% @user.songs.each do |song| %>
  <%= song.name %>
  <% if can? :remove_song, song %>
    <%= link_to 'Remove song', remove_song_song_path(song), method: :delete %>
  <% end %>
<% end %> -->

<% @user.user_songs.each do |user_song| %>
  <%= user_song.song.name %>
  <% if can? :delete, user_song %>
    <%= link_to 'Remove song', remove_song_song_path(user_song.song), method: :delete %>
  <% end %>
<% end %>
